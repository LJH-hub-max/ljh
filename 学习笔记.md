# 学习笔记

> 课程设计：基于 RAG 的《数据结构》课程智能问答系统  
> 作者：刘家豪
> 日期：2026-08-25

---

## 一、AI 工具的学习

### 1.1 Vibe Coding（AI 编程）体验

Vibe Coding 是一种借助大语言模型进行辅助编程的开发模式。在本次课程设计中，我主要使用了以下 AI 工具：

- **GitHub Copilot**：在 VS Code 中提供代码补全、函数生成、注释翻译等功能。在编写文档解析、向量化等模块时，Copilot 自动生成了大量样板代码，节省了约 40% 的编码时间。
- **Cursor**：基于 AI 的代码编辑器，支持用自然语言描述需求直接生成代码。例如，输入“用 Python 读取 PDF 并分块”，Cursor 直接生成了基于 PyPDF2 和 langchain 的分块代码，稍作修改即可使用。
- **ChatGPT / DeepSeek**：用于算法思路梳理、代码解释、调试建议以及文档生成。在搭建 RAG 流程时，通过对话明确了检索 top-k 的选择和提示词模板的设计。

**学习心得：**
- AI 工具能显著加速开发，但生成的代码必须经过人工审查和测试，尤其是涉及路径、编码和异常处理的部分。
- 有效的提示词（Prompt）设计是提高 AI 输出质量的关键。例如，明确指定“使用 langchain 的 RecursiveCharacterTextSplitter，chunk_size=500, chunk_overlap=50”，可以生成更准确的代码。
- Vibe Coding 的核心是开发者专注于整体设计和验证，将重复性编码交给 AI，而不是完全依赖 AI。

### 1.2 拟选用的 Harness（评估框架）和模型

在课程设计中，我计划构建一个基于大语言模型的《数据结构》课程智能问答系统。拟选用以下组件：

- **Harness（应用编排框架）**：选用 **LangChain** 作为主要框架，负责文档加载、文本分割、向量存储、检索链和提示模板管理。同时使用 **RAGAS** 作为评估框架，对问答质量进行自动化评估。
- **模型选择**：
  - **本地模型**：使用 **Ollama** 部署 `qwen2.5:7b-instruct`，用于离线推理和隐私保护，避免将课程资料上传到外部 API。
  - **云端 API**（备选）：使用 **DeepSeek-V3**，用于需要更强推理能力的场景或本地资源不足时。
- **嵌入模型**：选用 `bge-large-zh-v1.5`（通过 sentence-transformers 加载），中文语义表示效果好，且支持本地运行。
- **向量数据库**：使用 **Chroma**，轻量级、易集成，适合课程设计规模的数据集。
- **评估方式**：使用 **RAGAS** 框架计算忠实度（faithfulness）、答案相关性（answer relevance）和上下文召回率（context recall），并结合人工打分。

---

## 二、Git 原理与功能

### 2.1 Git 是什么？

Git 是一个分布式版本控制系统，用于跟踪文件的更改，协调多人协作开发。在本次课程设计中，我使用 Git 管理项目代码、文档和实验记录。

### 2.2 核心原理

- **快照，而非差异**：Git 每次提交保存的是所有文件的完整快照（通过指针引用），而非文件间的差异。这使得版本切换非常快速。
- **本地操作**：几乎所有操作都在本地完成，速度快且支持离线工作。
- **完整性**：所有数据在存储前都进行 SHA-1 哈希校验，确保内容不被篡改。
- **三种状态**：工作区（Working Directory）、暂存区（Staging Area）、仓库（Repository）。文件在工作区修改，通过 `git add` 进入暂存区，通过 `git commit` 提交到仓库。

### 2.3 常用功能及命令

| 功能 | 命令示例 | 说明 |
|------|----------|------|
| 初始化仓库 | `git init` | 在当前目录创建新仓库 |
| 克隆远程仓库 | `git clone https://github.com/zhangsan/data-structure-qa.git` | 下载远程仓库到本地 |
| 查看状态 | `git status` | 查看工作区与暂存区状态 |
| 添加文件 | `git add .` | 将所有更改添加到暂存区 |
| 提交更改 | `git commit -m "实现文档加载和分块模块"` | 将暂存区内容提交到本地仓库 |
| 推送远程 | `git push origin main` | 将本地提交推送到远程 main 分支 |
| 拉取更新 | `git pull` | 拉取远程更新并合并到本地 |
| 分支管理 | `git checkout -b feature/rag` | 创建并切换到新分支 |
| 合并分支 | `git merge feature/rag` | 将分支合并到当前分支 |
| 查看历史 | `git log --oneline --graph --all` | 查看所有分支的提交历史 |

### 2.4 在课程设计中的作用

- 记录每次代码修改，方便回滚到之前的稳定版本。
- 使用分支管理实验不同方案：`main` 分支保存稳定代码，`feature/rag` 分支开发检索增强功能，`feature/fine-tune` 分支尝试微调（后来放弃）。
- 与队友协作时避免代码冲突，通过 Pull Request 进行代码审查。

---

## 三、选题调研

### 3.1 选题背景

《数据结构》课程知识点多、概念抽象，学生在复习时经常遇到疑问，但教师无法随时解答。传统搜索引擎难以理解复杂语义（例如“红黑树和 AVL 树的区别”），而通用大语言模型（如 ChatGPT）缺乏课程特定的术语和讲义内容，容易产生幻觉。因此，我选择 **“基于 RAG 的《数据结构》课程智能问答系统”** 作为课程设计题目，旨在构建一个能够基于课程讲义和教材回答学生问题的系统。

### 3.2 调研内容

#### 3.2.1 相关技术

- **检索增强生成（RAG）**：结合外部知识库与 LLM，先检索相关文档片段，再让 LLM 基于检索结果生成回答，提高准确性和可解释性。
- **向量数据库**：如 FAISS、Chroma、Milvus，用于存储和检索文本嵌入向量。
- **嵌入模型**：`bge-large-zh`、`text-embedding-3-small` 等，将文本转换为高维向量。
- **文档解析与分块**：使用 `PyPDF2`、`python-docx` 解析 PDF 和 Word 文档，使用 `RecursiveCharacterTextSplitter` 进行语义分块。

#### 3.2.2 已有方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| 纯 LLM 问答 | 实现简单，无需知识库 | 易产生幻觉，知识陈旧，无法回答课程特定问题 |
| 微调模型 | 定制化强，能学习课程风格 | 成本高，需大量标注数据，更新知识需重新训练 |
| RAG 方案 | 知识可更新，可解释性强，成本可控 | 架构复杂，需维护向量库和检索质量 |

**结论**：采用 RAG 方案，结合本地模型部署，既保证了回答的准确性，又避免了数据隐私问题，且成本可控。

#### 3.2.3 系统设计概要

- **数据源**：课程 PPT（PDF 格式）、教材相关章节、常见问题 FAQ（人工整理）。
- **流程**：
  1. 文档解析：使用 PyPDF2 提取 PDF 文本。
  2. 文本分块：使用 LangChain 的 `RecursiveCharacterTextSplitter`，chunk_size=500，chunk_overlap=50。
  3. 向量化：使用 `bge-large-zh-v1.5` 将文本块转换为向量。
  4. 存入向量库：使用 Chroma 存储向量和原始文本。
  5. 用户提问：前端输入问题。
  6. 检索相关块：使用余弦相似度检索 top-5 相关文本块。
  7. 拼接提示词：将检索到的文本块和用户问题填入提示模板。
  8. LLM 生成回答：使用 Ollama 部署的 qwen2.5:7b 生成回答。
- **评估**：使用 RAGAS 指标 + 人工打分，对 30 个测试问题进行评估。

---

## 四、总结与展望

通过本次学习，我掌握了：
- Git 的基本原理和常用操作，能够管理代码版本并进行分支协作。
- 使用 AI 编程工具（Copilot、Cursor）提升开发效率，并理解了 Vibe Coding 的适用场景和局限。
- 完成了选题调研，确定了基于 RAG 的课程问答系统方案，并搭建了基础框架。

下一步计划：
1. 完善文档加载和分块模块，支持更多格式（如 PPTX）。
2. 优化检索策略，尝试混合检索（BM25 + 向量）。
3. 开发简单的 Web 界面（使用 Streamlit），完成系统集成与测试。
4. 使用 RAGAS 进行自动化评估，并根据结果调优。

---

*注：本笔记为课程设计过程记录，会持续更新。*
