# 方案设计

## 1. 功能需求分析
系统面向离散制造车间的生产计划员与车间管理人员，需提供以下核心功能模块：

| 功能模块 | 详细说明 |
| :--- | :--- |
| **数据管理** | 支持导入订单信息（工件类型、数量、交期）、设备信息（机器列表、加工能力）、工艺路线（工序顺序及可选机器）。提供数据预览、编辑、校验功能，确保输入数据的完整性与合法性。 |
| **静态排产** | 基于当前车间状态，一键生成最优初始调度方案，以甘特图形式可视化展示各机器上的工序安排。支持用户自定义优化目标（最小化makespan、最小化总拖期、最大化机器利用率等）。 |
| **动态重调度** | 当发生紧急插单、设备故障、订单取消等扰动时，系统自动触发重调度，给出调整后的调度方案，并标注变动部分（高亮显示新增/调整/删除的工序），便于用户快速识别变化。 |
| **方案对比** | 支持多种算法（GA、PSO-GA、禁忌搜索等）的调度结果对比，展示最大完工时间、总拖期、机器利用率等关键指标，以雷达图和多指标柱状图呈现，辅助用户选择最优方案。 |
| **智能报告** | 基于大模型自动生成调度方案说明报告，包括排产逻辑、关键路径分析及异常应对建议，支持自然语言问答，用户可输入如"为什么机器3负载这么高？"等问题，系统给出解释。 |
| **历史记录** | 表格展示历史调度记录，包括调度时间、方案指标、操作人员等，支持查看详情和重新执行历史方案，便于复盘与经验积累。 |

## 2. 方案论证

| 方案 | 描述 | 优势 | 劣势 |
| :--- | :--- | :--- | :--- |
| **方案一：精确求解法（分支定界/动态规划）** | 建立MILP数学模型，使用精确算法求解FJSP问题。 | 理论上可找到全局最优解，解的质量有数学保证。 | 求解时间随问题规模呈指数增长，10×10以上的实例即难以在合理时间内完成求解，无法应对动态场景。 |
| **方案二：单一启发式算法（如GA或PSO）** | 单独使用一种元启发式算法进行求解。 | 实现相对简单，可处理中等规模问题，求解速度较快。 | 单一算法易陷入局部最优，收敛速度与解质量难以兼顾，缺乏针对不同问题特征的自适应能力。 |
| **方案三：多策略融合智能优化（本方案）** | 以遗传算法为主体框架，融合粒子群算法的自适应参数调节机制和禁忌搜索的局部深度搜索能力，形成混合优化策略；同时引入Q-learning实现动态环境下的策略自适应。 | 兼顾全局探索与局部开发能力，适应静态与动态场景，求解质量与效率均衡，具有良好的可扩展性。 | 算法设计复杂度较高，需合理配置各模块间的协同逻辑和参数设置。 |

**论证结论**：虽然精确求解法在理论上具有最优性保证，但在实际工业场景中，柔性作业车间的规模通常远超其可解范围，且动态扰动频繁发生，使得精确方法几乎不可行。单一启发式算法虽然效率较高，但解的质量不稳定。综合考虑求解质量、计算效率和动态适应性，本方案选择方案三——多策略融合智能优化，通过多种算法的优势互补，在工程可行性与解的质量之间取得最佳平衡。

## 3. 方案规划

### 3.1 技术栈（Tech Stack）

| 层级 | 技术选型 | 选型理由 |
| :--- | :--- | :--- |
| **编程语言** | Python 3.9+ | 丰富的AI/科学计算库生态，开发效率高，适合算法原型快速实现 |
| **后端框架** | Flask（提供RESTful API接口） | 轻量级、灵活，适合构建中小型Web服务的后端 |
| **前端框架** | Vue 3 + Element Plus（UI组件库） | 响应式设计、组件丰富、开发效率高 |
| **可视化库** | ECharts 5（甘特图、对比图表） | 功能强大、社区活跃、图表类型齐全 |
| **智能优化算法** | 自行实现GA、PSO、Tabu Search、VNS | 灵活性高，便于算法改进和定制化设计 |
| **机器学习库** | scikit-learn（随机森林） | 成熟稳定、接口统一、易于使用 |
| **深度学习框架** | PyTorch 1.12+（BP神经网络） | 动态图机制便于调试，学术和工业界广泛使用 |
| **强化学习** | 自行实现Q-learning（离散状态-动作空间） | 调度问题状态动作空间相对有限，无需深度强化学习 |
| **大模型接口** | DeepSeek API / 本地Qwen-7B部署 | 成本可控、中文理解能力强 |
| **数据库** | SQLite + Redis | SQLite轻量无需单独部署，Redis用于缓存高频查询结果 |

### 3.2 需配置的外部资源

- **硬件资源**：本地开发环境（CPU: i5及以上，内存16GB+），如调用本地大模型需配置NVIDIA GPU（8GB显存以上）。
- **API资源**：大模型API密钥（DeepSeek或OpenAI），用于报告生成与自然语言问答功能。
- **数据资源**：公开的FJSP标准测试数据集（Brandimarte数据集、Kim数据集）以及实际车间仿真数据。
- **依赖库**：详见 `requirements.txt`（将在开发阶段生成），主要包括Flask、torch、scikit-learn、pandas、numpy、echarts等。

### 3.3 数据集来源

| 数据类型 | 来源说明 | 用途 |
| :--- | :--- | :--- |
| **FJSP标准测试集** | **Brandimarte数据集**（10个实例，规模从4台机器×5个工件到15台机器×10个工件）、**Kim数据集**、**Hurink数据集**，以上均为柔性作业车间调度领域广泛使用的基准测试集。 | 用于算法性能测试与对比验证，确保调度算法的有效性和通用性。 |
| **加工时间预测数据** | 基于公开的铣削/车削加工参数仿真生成，提取工件材料、刀具类型、工序参数等特征，构建加工时间预测训练集。 | 训练和验证BP神经网络及随机森林加工时间预测模型的精度。 |
| **动态扰动场景数据** | 通过仿真生成紧急插单、机器故障等扰动事件序列。 | 用于测试动态重调度模块的响应效果和策略选择的合理性。 |

### 3.4 前端UI设计方案

**整体布局**：左侧固定导航菜单，右侧自适应主内容区。

| 页面 | 核心内容与交互 |
| :--- | :--- |
| **工作台（Dashboard）** | 展示当前车间状态概览：待加工订单数、设备稼动率、今日拖期订单数、最近调度方案执行进度，以卡片和迷你图表呈现关键指标。 |
| **数据管理** | 表格形式展示订单列表和设备列表，支持增删改查操作；支持CSV/Excel批量导入导出；提供数据合法性校验和错误提示。 |
| **调度中心** | 核心交互区：点击"生成调度"按钮后，进度条显示算法运行进度；结果以交互式甘特图呈现（鼠标悬停显示工序详情，支持拖拽调整视图）；用户可选择不同的优化目标（最小化makespan/总拖期/机器负载均衡）。 |
| **方案对比** | 以雷达图和多指标柱状图展示不同算法的调度结果，支持勾选对比方案，直观展示各算法在不同指标上的优劣。 |
| **智能报告** | 展示大模型生成的调度说明报告，包括方案摘要、关键路径分析、设备利用率分析、改进建议等；提供对话输入框，用户可输入自然语言问题，系统返回解答。 |
| **历史记录** | 表格展示历史调度记录（含方案快照），支持查看详情、重新执行和方案删除操作。 |

### 3.5 后端算法核心逻辑

#### 3.5.1 静态排产核心算法（混合优化框架）

#### 3.5.2 动态重调度机制（Q-learning驱动）

- **状态空间**：当前调度完成率、故障机器编号、待重调度工序数量、平均工序延迟时间。
- **动作空间**：完全重调度、局部修复、右移重调度、插入调整。
- **奖励函数**：`Reward = -(延误时间增量 + 机器负荷方差增量)`
- **策略**：ε-greedy策略，ε初始0.3，随迭代次数衰减至0.05。

#### 3.5.3 加工时间预测模块（BP神经网络 + 随机森林）

- **BP神经网络**：3层隐藏层（64→128→64），ReLU激活函数，Dropout防止过拟合。
- **随机森林**：作为对比基线和辅助验证，提供特征重要性排序。
- **输入特征**：工件材料硬度、工序类型（车/铣/钻/磨）、刀具直径、切削深度、进给速度。
- **输出**：预测加工时间（分钟）。

#### 3.5.4 大模型报告生成模块

- 将调度方案的JSON结果（工序分配、时间安排、关键指标）和车间异常信息（如有）结构化组织后构造Prompt，调用大模型API生成可读性强的报告。
- 支持多轮对话，维护对话历史上下文。
