# 方案设计（基于深度强化学习的柔性作业车间智能排产系统）

## 1. 功能需求分析
系统面向离散制造车间的生产计划员与车间管理人员，需提供以下核心功能模块（与原始需求保持一致，但底层算法支撑升级）：

| 功能模块 | 详细说明 |
| :--- | :--- |
| **数据管理** | 支持导入订单信息（工件类型、数量、交期）、设备信息（机器列表、加工能力）、工艺路线（工序顺序及可选机器）。提供数据预览、编辑、校验功能，确保输入数据的完整性与合法性。 |
| **静态排产** | 基于当前车间状态，一键生成最优初始调度方案，以甘特图形式可视化展示各机器上的工序安排。支持用户自定义优化目标（最小化makespan、最小化总拖期、最大化机器利用率等）。 |
| **动态重调度** | 当发生紧急插单、设备故障、订单取消等扰动时，系统自动触发重调度，给出调整后的调度方案，并标注变动部分（高亮显示新增/调整/删除的工序），便于用户快速识别变化。 |
| **方案对比** | 支持多种策略（DRL、GA、PSO-GA、禁忌搜索等）的调度结果对比，展示最大完工时间、总拖期、机器利用率等关键指标，以雷达图和多指标柱状图呈现，辅助用户选择最优方案。 |
| **智能报告** | 基于大模型自动生成调度方案说明报告，包括排产逻辑、关键路径分析及异常应对建议，支持自然语言问答，用户可输入如"为什么机器3负载这么高？"等问题，系统给出解释。 |
| **历史记录** | 表格展示历史调度记录，包括调度时间、方案指标、操作人员等，支持查看详情和重新执行历史方案，便于复盘与经验积累。 |

## 2. 方案论证

| 方案 | 描述 | 优势 | 劣势 |
| :--- | :--- | :--- | :--- |
| **方案一：精确求解法（分支定界/动态规划）** | 建立MILP数学模型，使用精确算法求解FJSP问题。 | 理论上可找到全局最优解，解的质量有数学保证。 | 求解时间随问题规模呈指数增长，10×10以上的实例即难以在合理时间内完成求解，无法应对动态场景。 |
| **方案二：单一启发式算法（如GA或PSO）** | 单独使用一种元启发式算法进行求解。 | 实现相对简单，可处理中等规模问题，求解速度较快。 | 单一算法易陷入局部最优，收敛速度与解质量难以兼顾，缺乏针对不同问题特征的自适应能力。 |
| **方案三：传统混合优化（GA+PSO+Tabu）** | 以遗传算法为主体，融合PSO自适应调参和禁忌搜索局部优化。 | 兼顾全局探索与局部开发，解质量优于单一算法。 | 仍属离线优化方法，实时响应能力有限；对新场景泛化性不足，每次扰动需重新计算。 |
| **方案四：深度强化学习调度（本方案）** | 基于**多指针图网络（MPGN）** 构建端到端策略网络，将FJSP建模为**多动作空间的马尔可夫决策过程（MMDP）**，采用**多近端策略优化（multi-PPO）** 算法训练作业操作选择策略和机器选择策略。训练后可**零样本泛化**到不同规模实例，且决策时间极短（毫秒级），天然适配动态重调度。 | ① 端到端决策，无需手工设计调度规则；② 训练后可秒级响应新实例，满足实时性要求；③ 泛化能力强，可处理任意规模的作业/机器数；④ 结合图神经网络（GNN）嵌入析取图状态，充分利用结构信息。 | 训练阶段计算成本高，需大量仿真数据；策略性能受训练场景覆盖度影响；可解释性较弱。 |

**论证结论**：精确求解法不适用于工业规模；传统启发式和混合优化虽在静态场景有效，但面对频繁动态扰动时重计算耗时过长，且每次需从头优化。深度强化学习方法通过离线训练获得通用策略，在线推理时仅需前向传播即可快速输出决策，同时具备对未见实例的泛化能力，非常适合实际车间环境中频繁变动的需求。因此，本方案选择**方案四——深度强化学习调度架构**作为核心排产引擎，并辅以传统算法作为对比基准。

## 3. 方案规划

### 3.1 技术栈（Tech Stack）

| 层级 | 技术选型 | 选型理由 |
| :--- | :--- | :--- |
| **编程语言** | Python 3.9+ | 丰富的AI/科学计算库生态，开发效率高，适合算法原型快速实现 |
| **后端框架** | Flask（提供RESTful API接口） | 轻量级、灵活，适合构建中小型Web服务的后端 |
| **前端框架** | Vue 3 + Element Plus（UI组件库） | 响应式设计、组件丰富、开发效率高 |
| **可视化库** | ECharts 5（甘特图、对比图表） | 功能强大、社区活跃、图表类型齐全 |
| **深度强化学习框架** | PyTorch 1.12+（实现MPGN、multi-PPO） | 动态图机制便于调试，支持自定义图神经网络层，学术和工业界广泛使用 |
| **图神经网络库** | PyTorch Geometric（用于析取图嵌入） | 提供高效的图卷积、图注意力等操作，简化图网络实现 |
| **传统优化算法** | 自行实现GA、PSO、Tabu Search（作为对比基线） | 便于与DRL结果进行对比分析 |
| **大模型接口** | DeepSeek API / 本地Qwen-7B部署 | 成本可控、中文理解能力强 |
| **数据库** | SQLite + Redis | SQLite轻量无需单独部署，Redis用于缓存高频查询结果 |

### 3.2 需配置的外部资源

- **硬件资源**：训练阶段需配置NVIDIA GPU（建议显存≥16GB，如RTX 3080/4090），推理阶段CPU即可满足（i7及以上，内存16GB+）。
- **API资源**：大模型API密钥（DeepSeek或OpenAI），用于报告生成与自然语言问答功能。
- **数据资源**：公开的FJSP标准测试数据集（Brandimarte、Kim、Hurink等）以及实际车间仿真数据（用于训练DRL智能体）。
- **依赖库**：详见 `requirements.txt`（将在开发阶段生成），主要包括Flask、torch、torch-geometric、scikit-learn、pandas、numpy、echarts等。

### 3.3 数据集来源

| 数据类型 | 来源说明 | 用途 |
| :--- | :--- | :--- |
| **FJSP标准测试集** | **Brandimarte数据集**（10个实例，规模从4台机器×5个工件到15台机器×10个工件）、**Kim数据集**、**Hurink数据集**，以上均为柔性作业车间调度领域广泛使用的基准测试集。 | 用于算法性能测试与对比验证，同时作为DRL训练的验证集和测试集。 |
| **训练数据生成** | 通过随机生成大量不同规模（作业数5~30，机器数3~15）的FJSP实例，包含随机加工时间和工艺约束。 | 用于DRL智能体的训练（离线仿真环境）。 |
| **动态扰动场景数据** | 通过仿真生成紧急插单、机器故障等扰动事件序列。 | 用于测试动态重调度模块的响应效果和策略适应能力。 |
| **实际生产数据** | 合作企业提供的脱敏生产记录（含实际加工时间、设备日志等）。 | 用于模型微调和实际场景验证。 |

### 3.4 前端UI设计方案

（与原始方案一致，此处略，可参考原始文件内容）

### 3.5 后端算法核心逻辑（基于DRL）

#### 3.5.1 静态排产核心算法（深度强化学习调度器）

- **问题建模**：将FJSP建模为**多动作空间马尔可夫决策过程（MMDP）**，其中：
  - **状态**：使用**析取图**表示当前调度状态，包含工序节点、机器节点、已调度/未调度关系。通过**图神经网络（GNN）** 将局部状态嵌入为固定长度向量。
  - **动作空间**：层次化结构——首先从所有可调度的作业操作中选择一个操作（作业操作动作），然后为该操作从兼容机器中选择一台机器（机器动作）。
  - **奖励**：以负的makespan增量（或总拖期增量）作为即时奖励，引导智能体朝向优化目标。

- **策略网络架构（MPGN）**：采用**多指针图网络（Multi-Pointer Graph Network）**，包含两个编码器-解码器组件：
  - **作业操作策略网络**：输出各候选作业操作的选择概率。
  - **机器策略网络**：针对选定的作业操作，输出各兼容机器的选择概率。
  - 网络结构利用图注意力层聚合析取图节点信息，并利用指针机制处理可变大小的动作空间。

- **训练算法（Multi-PPO）**：采用**多近端策略优化（Multi-PPO）** 同时优化两个子策略，利用优势函数估计和裁剪目标，保证训练稳定性和样本效率。
  - 使用**小规模问题（如5×5）** 进行训练，利用GNN的置换不变性泛化至大规模问题。
  - 训练完成后，推理阶段仅需一次前向传播即可输出完整调度（逐步决策），耗时在毫秒级。

#### 3.5.2 动态重调度机制（DRL在线适配）

- **触发条件**：同原始方案（紧急插单、机器故障、订单取消、实际加工时间偏离）。
- **重调度策略**：
  - 将扰动后的新状态（含当前部分已调度工序）作为初始状态输入训练好的DRL策略网络，**继续决策未调度的工序**，实现局部/完全重调度。
  - 无需重新训练，策略网络可直接适应新场景，响应时间极短。
  - 若扰动较大（如机器永久故障），可基于当前状态**重置并重新开始调度**，同样利用DRL快速生成新方案。
- **Q-learning选项**：作为补充，可训练一个高层控制器（Q-learning）用于选择重调度模式（完全重调度、局部修复、右移重调度），但本方案中DRL本身即可实现自适应，故不以Q-learning为必须。

#### 3.5.3 加工时间预测模块（BP神经网络 + 随机森林）

- 与原始方案相同，作为辅助模块，为DRL提供准确的加工时间输入。
- 训练数据来源于历史加工记录或仿真生成。
- 预测精度目标：MAPE ≤ 10%。

#### 3.5.4 大模型报告生成模块

- 与原始方案一致，利用大模型生成报告和对话问答。

### 3.6 系统接口设计

（与原始方案基本一致，但调度接口可增加 `algorithm` 参数支持选择DRL/GA/PSO等）

| 接口名称 | 请求方式 | 端点 | 功能描述 |
| :--- | :--- | :--- | :--- |
| 数据上传 | POST | /api/data/upload | 上传订单、设备、工艺数据文件 |
| 数据查询 | GET | /api/data/list | 获取当前所有数据记录 |
| 静态排产 | POST | /api/schedule/generate | 触发调度算法（可选DRL/GA等），返回调度结果 |
| 动态重调度 | POST | /api/schedule/reschedule | 输入扰动信息，触发重调度（DRL快速响应） |
| 方案对比 | GET | /api/schedule/compare | 获取多算法（DRL vs GA等）对比结果数据 |
| 指标查询 | GET | /api/metrics | 获取当前调度方案的关键指标 |
| 报告生成 | POST | /api/report/generate | 调用大模型生成排产报告 |
| 智能问答 | POST | /api/chat | 提交自然语言问题，返回大模型回答 |
| 历史记录 | GET | /api/history/list | 获取历史调度方案列表 |

## 4. 技术路线图

> 以下提供Mermaid流程图（若未渲染请参考纯文本版）。

```mermaid
graph TD
  A[数据输入层] --> A1[订单/设备/工艺数据]
  A --> A2[历史加工数据]
  A --> A3[动态扰动事件]
  
  A2 --> B[加工时间预测模块]
  B --> B1[BP神经网络]
  B --> B2[随机森林]
  B1 --> C
  B2 --> C
  
  A1 --> C[DRL调度训练环境<br>（FJSP仿真器）]
  C --> C1[状态提取：析取图构建]
  C1 --> C2[MPGN策略网络<br>（作业操作策略 + 机器策略）]
  C2 --> C3[Multi-PPO训练<br>（离线训练，小规模泛化）]
  C3 --> D[训练好的DRL策略网络]
  
  A1 --> E[在线推理引擎]
  D --> E
  E --> F[初始调度方案（静态）]
  
  A3 --> G[动态扰动检测]
  G --> E
  E --> H[更新调度方案（重调度）]
  
  F --> I[前端可视化看板]
  H --> I
  I --> I1[甘特图展示]
  I --> I2[关键指标仪表盘]
  I --> I3[方案对比分析（含传统算法）]
  
  I --> J[大模型报告生成]
  J --> J1[排产说明报告]
  J --> J2[自然语言问答]
  
  K[传统优化算法<br>（GA/PSO/Tabu）] --> I3
  K --> C[训练环境对比基线]
